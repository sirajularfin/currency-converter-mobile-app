import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabBar: {
    backgroundColor: '#fff',
    borderTopColor: '#ddd',
    height: 60,
    paddingBottom: 8,
    cursor: 'pointer',
  },
  tabBarLabel: {
    fontSize: 12,
  },
});

export default styles;
